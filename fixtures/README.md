# Fixtures sintéticas

As fixtures existem apenas para o harness isolado. Binários em `bin/` são
doubles controlados e os arquivos em `documentation/` usam exclusivamente
valores sintéticos ou reservados.

Nenhuma fixture representa uma credencial, chave, instalação ou endpoint real.
